# Staging is the customer preview/testing site: it should behave like prod.
# Without this, Symfony treats every env except "prod" as a debug env
# (see Dotenv::bootEnv()), which would expose stack traces & slow the site down.
###> symfony/framework-bundle ###
APP_DEBUG=0
###< symfony/framework-bundle ###
