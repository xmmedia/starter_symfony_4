CountryQuery:
    decorator: true
    heirs: [Query]
    config:
        fields:
            Countries:
                type: '[Country!]!'
                description: 'Retrieve all Countries.'
                public: '@=isAuthenticated()'
                access: '@=isAuthenticated()'
                resolve: '@=resolver("App\\Infrastructure\\GraphQl\\Resolver\\CountryResolver")'
