{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ vite_entry_link_tags('user') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ vite_entry_script_tags('user', { attr: { 'data-entry': 'user' } }) }}
{% endblock %}

{% block body %}
    {# #app is what Vue.js mounts to #}
    <div id="app">
        {% include 'flash_messages.html.twig' %}

        {% include 'loading.html.twig' %}
    </div>
{% endblock body %}
