#Upload:
#    type: custom-scalar
#    config:
#        description: 'A file upload.'
#        scalarType: '@=newObject("Overblog\\GraphQLBundle\\Upload\\Type\\GraphQLUploadType")'

#FileInput:
#    type: input-object
#    config:
#        description: 'File add & change mutation input. Allows for adding a new file, replacing an existing, or keeping an existing file.'
#        fields:
#            file:
#                type:        Upload
#                description: 'If uploading a file, this field should be a reference to the file blob in the Apollo style.'
#            filename:
#                type:        String
#                description: 'If keeping the existing file, this field should be the name of the existing file, typically <uuid>.<ext>.'
