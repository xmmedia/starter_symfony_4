query GetTemplatesQuery {
    Templates {
        template
        name
        default
        items {
            item
            name
            type
            required
            config
        }
    }
}
