query GetTemplatesQuery {
    Templates {
        template
        name
        editMetaDescription
        items {
            item
            name
            type
            required
            config
        }
    }
}
