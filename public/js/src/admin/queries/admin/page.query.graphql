query GetPagesQuery {
    Pages {
        pageId
        path
        title
    }
}

query GetPageQuery($pageId: UUID!) {
    Page(pageId: $pageId) {
        pageId
        path
        template {
            template
        }
        title
        published
        content
    }
}
